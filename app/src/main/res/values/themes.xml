<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SearchGame" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/skyBlue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonColor" parent="ThemeOverlay.AppCompat">
        <item name="colorPrimary">@color/buttonLightColor</item>
        <item name="colorOnPrimary">@color/black</item>
    </style>

    <style name="TextColor" parent="ThemeOverlay.AppCompat">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="BottomButtonColor" parent="ThemeOverlay.AppCompat">
        <item name="colorPrimary">@color/skyBlue</item>
        <item name="colorOnPrimary">@color/black</item>
    </style>

</resources>